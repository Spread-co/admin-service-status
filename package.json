{
  "name": "admin-service-status",
  "version": "0.1.6",
  "description": "Admin service status alert panel for Spread.co — create, approve, send, and resolve service alerts",
  "scripts": {
    "build": "weweb build -- name=admin-service-status type=element",
    "serve": "weweb serve"
  },
  "devDependencies": {
    "@weweb/cli": "^3.8.0"
  }
}
